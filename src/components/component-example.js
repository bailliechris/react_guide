//import React from 'react'
// Import PropTypes to define types - throws errors in console but not much else?
/*

import PropTypes from 'prop-types'

const header = (props) => {
    return (
        <header>
            <h1>{props.title}</h1>
        </header>
    )
}

// Set Default Props
header.defaultProps = {
    title: "Default"
}

// Setting proptypes (needs header above)
header.propTypes = {
    title: PropTypes.string
}

// CSS as Styles here or inline in html tageg - style={{color:'red', backgroundColor:'yellow'}}
const headingStyle = {
    color:'red', backgroundColor:'yellow'

}

export default header

*/